from random import randint


class GameLogic:

    # kind of its own thing, static method has input and has outputs, just needs a place to live

    @staticmethod
    def roll_dice(num_dice):
        """
        param num_dice
        return tuple of interger values between 1 and 6
        """
        """
        random_nums=[1] is a list
        return tuple(random_nums) turns our list into a tupple
        """
        return 1,
